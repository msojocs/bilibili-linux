> [!NOTE]
> This file is generated by AI.

**[Simplified Chinese](README.MD) | [Traditional Chinese](README_zh-tw.MD) | English**

<div align="center">

  <img src="./res/icons/bilibili.svg" height="100px" width="100px"/>

  <h3>Bilibili for Linux</h3>
  <br>

----

[![aur](https://img.shields.io/aur/version/bilibili-bin)](https://aur.archlinux.org/packages/bilibili-bin)

  This is Bilibili for Linux.

  SVG icon from [@Peternal](https://github.com/Peternal)

  <br>
</div>

## Introduction

This project involves:

1. Deobfuscation
2. Debugging
3. Brains

This project is completely open source, with no code encryption. If you have any doubts, please review the code yourself or stop using the related files.

## Features

1. No annoying title bar
2. Full-screen support for low-resolution screens
3. Update check support
4. A menu to close the application
5. Roaming support
6. Search in other regions
7. [Danmaku Sharing](docs/help/弹幕共享.MD) (Content in Chinese)

## Technology Stack

- TypeScript in strict mode, bundled with Vite
- React 19, Redux Toolkit, Ant Design, and Sass for the injected settings UI
- Electron 43 and electron-builder for the desktop client and packages
- pnpm 10 for dependency management
- A browser-side extension for page injection, request interception, and local storage, plus Electron-side integration patches

## Developer Guide

### Prerequisites

Install a current Node.js LTS release and pnpm 10. `corepack enable` is a convenient way to make pnpm available, then install dependencies:

```bash
pnpm install
```

NixOS users can enter the repository-provided development environment instead:

```bash
nix develop
```

Prepare the upstream client and its resources once:

```bash
tools/setup-bilibili.sh
```

The script downloads and unpacks the upstream client, builds the extension, and creates the runtime files under `app/`.

### Common Commands

```bash
pnpm dev       # Watch and rebuild the extension and injection bundles
pnpm build     # Type-check and create a production bundle
pnpm lint      # Run ESLint
bin/bilibili   # Launch the prepared client
pnpm pkg-linux # Build Linux packages
```

Most source work lives in `src/extension/` (page injection and settings UI) and `src/inject/` (Electron-side patches). Edit source files in `src/`, `res/`, `tools/`, or `conf/`; do not hand-edit generated `app/`, `dist/`, `tmp/`, or `cache/` content. Before submitting a change, run `pnpm build`, `pnpm lint`, and `git diff --check` when applicable. See [AGENTS.md](AGENTS.md) for the full contributor guidance.

## How to Use

### Automated Builds

Please download from the Release page:

https://github.com/msojocs/bilibili-linux/releases

For continuous builds:

https://github.com/msojocs/bilibili-linux/releases/tag/continuous

### Manual Build

1. Clone the code
   ```
   git clone https://github.com/msojocs/bilibili-linux.git
   cd bilibili-linux
   ```
2. Install
   ```
   tools/setup-bilibili.sh
   ```
3. Launch
   ```
   bin/bilibili
   ```
### AppImage Installation

Please use [AppImageLauncher](https://github.com/TheAssassin/AppImageLauncher)

## Gentoo Linux

Emerge from [gentoo-zh overlay](https://github.com/microcai/gentoo-zh/tree/master/media-video/bilibili).

### Additional Notes

If you don't want to install another Electron on your computer, you can extract `app.asar` from the release version and run it with your already installed Electron. The recommended Electron version is: `43.1.1` (`VaapiOnNvidiaGPUs` requires Electron 43+ / Chromium 130+).

### Local History Backup

History is stored as readable JSON at `history-backup.json` inside Electron's `app.getPath("userData")` directory. With the standard Linux profile, the exact file is `~/.config/bilibili/history-backup.json`. Entries are separated by account and ordered from newest to oldest. The file stores the original history objects directly; pagination keys and timestamps are derived when read rather than duplicated in every entry. Refreshing the first History page starts a throttled background scan of the remaining server pages; foreground scrolling always takes priority and receives the original server response. After a complete scan, `serverOldest` records the oldest server entry: local entries newer than that boundary are replaced by the current server snapshot, while older local entries are preserved unchanged. Those preserved entries continue after the live server history in the normal History page.

## GPU Acceleration Configuration

Electron 43 (Chromium 150) supports VA-API hardware video decoding on Linux. Below are the recommended configurations:

### NVIDIA GPU

1. Install the VA-API → NVDEC backend:
   ```bash
   # Arch Linux
   sudo pacman -S libva-nvidia-driver
   # Debian/Ubuntu
   sudo apt install libva-nvidia-driver
   ```

2. Set the environment variable (libva needs this to find the NVIDIA backend):
   ```bash
   export LIBVA_DRIVER_NAME=nvidia
   ```

3. Configure in `~/.config/bilibili/bilibili-flags.conf`:

   ```
   --ignore-gpu-blocklist
   --enable-features=AcceleratedVideoDecodeLinuxGL,AcceleratedVideoDecodeLinuxZeroCopyGL,VaapiOnNvidiaGPUs,VaapiIgnoreDriverChecks,PlatformHEVCDecoderSupport
   --enable-gpu-rasterization
   --enable-zero-copy
   --disable-gpu-sandbox
   ```

   > **Notes**:
   > - `VaapiOnNvidiaGPUs` (Chromium 130+) bypasses vaapi_wrapper's NVIDIA device skip check
   > - `AcceleratedVideoDecodeLinuxGL` / `AcceleratedVideoDecodeLinuxZeroCopyGL` are the recommended GL zero-copy decode paths for Chromium 150
   > - Electron 28 lacks these features — NVIDIA hardware video decode requires Electron 43+

### Intel / AMD GPU

```
--ignore-gpu-blocklist
--enable-features=AcceleratedVideoDecodeLinuxGL,AcceleratedVideoDecodeLinuxZeroCopyGL,VaapiVideoDecoder,VaapiIgnoreDriverChecks,PlatformHEVCDecoderSupport
--enable-gpu-rasterization
--enable-zero-copy
--disable-gpu-sandbox
```

Intel iGPUs and AMD GPUs use the Mesa VA-API backend — no additional driver or environment variables needed.

### Verifying Hardware Decode

After playing a video, check if the GPU decode engine is active:

```bash
cat /proc/$(pgrep -f "type=gpu-process.*bilibili")/fdinfo/* | grep drm-engine-video
```

A non-zero value indicates the GPU decode engine is working. You can also visit `chrome://gpu` to check the "Video Decode" status.

### Note

- `--use-gl=angle --use-angle=gl` may break VA-API decoding on some Linux desktop environments (especially Wayland). The default EGL/GLX backend is recommended.
- If using a system proxy (e.g. Clash), `bilipc.bilibili.com` may fail to resolve through the proxy, causing a blank white screen. Electron 43+ includes built-in proxy bypass handling.

## Developer Tools

### F12 Developer Panel

You can now open the developer tools yourself:

Try pressing <kbd>F12</kbd>. If there's no response, it's not enabled.

The login interface has two layers: right-click to open the inner layer, and press <kbd>F12</kbd> for the outer layer.

## About Loongson

https://areweloongyet.com/docs/loong-or-loongarch

|Distribution|Architecture Identifier|
|---|---|
|AOSC OS|`loongarch64`|
|Debian|<ul><li>Old World: `loongarch64`</li><li>New World: `loong64`</li></ul>|
|Gentoo|`loong`|
|Loong Arch Linux|`loong64`|
|RPM-based|`loongarch64`|
|Slackware|`loong64`|

## SponsorBlock Assistant

Automatically helps you jump to key moments.

| Option | Description |
|---|---|
| Whisper Proxy | A proxy is needed for model downloads, e.g., `http://127.0.0.1:1080` |
| LD_LIBRARY_PATH | You might encounter a `cudnn` library not found issue, [refer here](https://github.com/MahmoudAshraf97/whisper-diarization/issues/259) |
| AI Recognition TOKEN | Token from https://www.bigmodel.cn/, using the free model `glm-4.5-flash` |

## Switch Language

1. Click the settings button in the bottom right corner of the homepage.
2. Go to "Other Settings".
3. Select your desired language in the "Language Settings" area.

## Changelog

[CHANGELOG.md](CHANGELOG.md)

## Preview

![Recommended](res/screenshots/1.png)

![Live](res/screenshots/live.png)

![Update Check](res/screenshots/update.png)

![Exit](res/screenshots/exit.png)

![Roaming](res/screenshots/roaming.png)

## Thanks to the following projects

1. [BiliRoaming](https://github.com/yujincheng08/BiliRoaming)
2. [bilibili-helper](https://github.com/ipcjs/bilibili-helper)
3. [BilibiliSponsorBlock](https://github.com/hanydd/BilibiliSponsorBlock)

## Disclaimer

The license applies only to this project, not its products.

The copyright of the Bilibili client belongs to Shanghai Kuanyu Digital Technology Co., Ltd.

The maintainer is not responsible for any additional issues arising from the use of this project, such as account bans or theft. Please use with caution.

If there are any improprieties, please contact me at: jiyecafe@gmail.com
