路由修改位置： `app/render/assets/index.50b7727f.js`

`case"SettingsPage":return r.push({name:"Settings"})}}`
```javascript
default:
  if(a)
  return r.push({
    name: a.page
  })
```

路由：
`const router`

`{'label':'首页','click':()=>this[eQ(0x47e)][eQ(0x228)]({'page':'Root'})},`